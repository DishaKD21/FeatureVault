/** Viewport width (px) at which diagram editor is enabled and theme toggle is shown. */
export const VIEWPORT_WIDE_MIN_PX = 800;

export const mediaMinWide = `(min-width: ${VIEWPORT_WIDE_MIN_PX}px)`;

export const mediaMaxNarrow = `(max-width: ${VIEWPORT_WIDE_MIN_PX - 1}px)`;
